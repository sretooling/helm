env:
  global:
    - PROJECT=helm

language: bash
services: docker

script:
  - make build
  - if [ "${TRAVIS_PULL_REQUEST}" == "master" ]; then make publish-version  ; fi
  - if [ "${TRAVIS_BRANCH}" == "master" -a "${TRAVIS_PULL_REQUEST}" == "false" ]; then make publish-latest ; fi
